<IfModule mod_rewrite.c>
    RewriteEngine On
    # ملفات الوسائط عبر Laravel مباشرة (قبل تمرير الطلب إلى public/storage كملف ثابت)
    RewriteRule ^(media|storage)/(.*)$ public/index.php [L,QSA]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
